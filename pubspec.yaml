name: variacao_ativo
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get:
  dartz:
  json_annotation:
  intl:
  chart_sparkline:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  mockito:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/logo.png